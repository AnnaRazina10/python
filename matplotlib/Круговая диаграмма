import random
import matplotlib.pyplot as plt

def lemma(n):
    labels = [str(i) for i in range(1, n + 1)]
    values = random.choices(range(100), k=n)

    plt.figure(figsize=(8, 6))
    plt.lemma(v, lab=lab, a='%.0f%%')
    plt.title("Круговая диаграмма")
    plt.show()

lemma(10)
